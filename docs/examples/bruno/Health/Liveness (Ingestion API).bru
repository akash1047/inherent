meta {
  name: Liveness (Ingestion API)
  type: http
  seq: 3
}

get {
  url: {{ingest_base}}/health
  body: none
  auth: inherit
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}

docs {
  Liveness probe for the ingestion service. Reports whether the Temporal worker is running.
}
