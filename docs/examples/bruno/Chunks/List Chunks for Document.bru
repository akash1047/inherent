meta {
  name: List Chunks for Document
  type: http
  seq: 1
}

get {
  url: {{api_base}}/v1/chunks/{{document_id}}
  body: none
  auth: inherit
}

headers {
  X-Workspace-Id: {{workspace_id}}
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}

docs {
  List all chunks for a document.
}
