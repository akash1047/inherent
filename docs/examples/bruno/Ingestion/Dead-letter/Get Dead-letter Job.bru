meta {
  name: Get Dead-letter Job
  type: http
  seq: 2
}

get {
  url: {{ingest_base}}/dead-letter/1
  body: none
  auth: inherit
}

tests {
  test("status is 200 or 404 (not found)", function() {
    expect([200, 404]).to.include(res.status);
  });
}

docs {
  Get a single dead-letter job by numeric `job_id`. 404 if not found. Replace `1` with a real job
  ID from the list.
}
