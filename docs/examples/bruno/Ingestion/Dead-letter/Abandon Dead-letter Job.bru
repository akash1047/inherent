meta {
  name: Abandon Dead-letter Job
  type: http
  seq: 4
}

post {
  url: {{ingest_base}}/dead-letter/1/abandon
  body: none
  auth: inherit
}

tests {
  test("status is 200 or 404 (not found)", function() {
    expect([200, 404]).to.include(res.status);
  });
}

docs {
  Mark a dead-letter job as permanently abandoned. 404 if the job is missing.
}
